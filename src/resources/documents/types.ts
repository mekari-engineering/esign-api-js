// src/resources/posts/types.ts

export declare type Document = {
  id: number;
  filename: string,
  signers: string[],
};

export declare type NewDocument = {
  doc: string;
  filename: string;
};
  